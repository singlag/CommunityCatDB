<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- 位置權限 -->
    <uses-permission android:name="android.Manifest.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.Manifest.permission.ACCESS_COARSE_LOCATION" />

    <!-- 相片權限 (Android 13+) -->
    <uses-permission android:name="android.Manifest.permission.READ_MEDIA_IMAGES" />
    <!-- 舊版 Android 相片權限 -->
    <uses-permission android:name="android.Manifest.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />



    <application
        android:name=".CatApplication"
        android:allowBackup="true"
        android:label="社區貓資料庫"
        android:theme="@style/Theme.CommunityCatDB"
        android:fullBackupContent="true">

        <!-- Google Maps API Key -->
        <!-- 為了安全，不要直接把 Key 寫在代碼裡。在專案根目錄的 local.properties 檔案中加入-->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <!-- FileProvider for Export -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>